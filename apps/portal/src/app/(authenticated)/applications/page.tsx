import ApplicationsListClient from './components/applications-list-client-refresh';

// Applications list page - server component that wraps the client list
export default function ApplicationsPage() {
    return <ApplicationsListClient />;
}
